# ROADMAP v4 — Merchant self-service portal

**You are done with v4 when** a seeded merchant user logs in through a real OIDC flow, lands on an authenticated dashboard, pages through their own bill history from **both** write paths with working filters, opens one bill and sees their own customer's contact details **while the public bill page still shows none**, edits and forks a template through the Phase-3 builder at an authenticated route with `merchantId` coming from the session, and logs out — after which the same cookie is dead; a second merchant's bill and template are **404** at every `/portal` endpoint; `SEED_MERCHANT_ID` exists in exactly one file; and **every v1, v2 and v3 test is still green**.

| ID | Step | Deliverable (one sitting) | Depends-on | Tier-1 | Verify locally |
|----|------|---------------------------|------------|--------|----------------|
| S-9 | Schema | Additive Prisma migration: `User` `+subject String? @unique`, `+disabledAt`, `+lastLoginAt`; new `MerchantSession` table (D-45). No column on `Merchant`, `Order`, `Bill`, or `Template` touched. | — | — | `pnpm prisma migrate dev` on a DB **containing existing v1/v2/v3 rows** succeeds; Studio shows the seeded `User` unchanged apart from nulls; **re-run the full v1+v2+v3 suites — all green**; no money, PII, or snapshot column touched. |
| A-1 | Auth seam | `IdentityProvider` port + OIDC RP adapter in `apps/api` via `openid-client` (authorization code + PKCE, `state`/`nonce` validated, JWKS-verified `id_token`). Local OP is `node-oidc-provider` as the **separate `apps/dev-idp` workspace app** — not a `docker compose` service (D-52, D-53) — with one seeded subject, started by `dev-up.ps1`. **No password field is added anywhere** (D-42). **Scope boundary, stated in the plan, the `apps/dev-idp` README, and a header comment on the RP adapter: `devInteractions` accepts ANY username/password — this task proves RP-side protocol handling, never that a login was legitimately verified (D-54).** | S-9 | **Tier-1 (authentication)** | `pnpm dev:up` starts the OP; a login round-trip returns a verified `id_token`; a tampered `id_token` signature, a replayed `state`, and a mismatched `nonce` each fail closed with no session written. **Isolation, asserted not assumed (D-53):** `node-oidc-provider` appears in no `package.json` outside `apps/dev-idp` (`grep -rn "node-oidc-provider" --include=package.json .` returns one file); adding `import "node-oidc-provider"` to any `apps/api` source file **fails typecheck** — try it, then revert; `apps/dev-idp` refuses to boot with `NODE_ENV=production`. |
| A-2 | Login | `/auth/login`, `/auth/callback`, `/portal/logout`: subject→`User` resolution, eligibility check (`EXTERNAL` + `merchantId` non-null + `disabledAt` null), session issuance, cookie set/clear, `revokedAt` on logout. | A-1 | **Tier-1 (authentication)** | Login → one `MerchantSession` row, cookie value ≠ stored `tokenHash`; logout → same cookie `401`s and the row survives with `revokedAt` set; an `INTERNAL` user, a null-merchant user, and a disabled user each get `403` with **zero** session rows written. |
| A-3 | Session guard | `SessionGuard` + `MerchantContext` (`{userId, merchantId, role}`): hash the cookie, load the session, re-check user eligibility on **every** request, expire and revoke. Role gate per `rbac.md` (D-50). | A-2 | **Tier-1 (tenancy root)** | Expired, revoked, unknown, and empty cookies each → `401`, cookie cleared; disabling the user mid-session kills the **next** request, not just the next login; a `STORE_STAFF` role hits a builder write route → `403`. |
| A-4 | Scoping audit | **Audit + confinement task:** find every `SEED_MERCHANT_ID` read (`DemoOnlyGuard` routes, the builder, `templates.service.ts`, seeds, scripts) and move all of them behind `MerchantContext`. Services take `merchantId` as an argument; the constant is read in the demo resolver only. Record the full inventory in `DECISIONS_v1.md` under D-46. | A-3 | **Tier-1 (tenancy root)** | `grep -rn "SEED_MERCHANT_ID" apps/ packages/ --include=*.ts` returns **one** non-test file; `/demo/*` behaves byte-identically to before (Phase-3 UAT re-run); no service signature reads an env var. |
| A-5 | CSRF | Double-submit CSRF token on every state-changing `/portal` route; cookie attributes hardened (`HttpOnly`, `SameSite=Lax`, `Secure` off local / on elsewhere by config, `Path=/`, no `Domain`). | A-3 | **Tier-1 (session integrity)** | A `POST` with a valid session cookie and no/ wrong CSRF token → rejected, `SELECT count(*)` unchanged; an HTML form on a different origin cannot fork a template; `GET`s are unaffected. |
| W-1 | Portal shell | `/portal` route group: layout, nav, logged-in merchant name, logout control, unauthenticated redirect to login. No business data. | A-3 | — | Hitting `/portal/*` logged out redirects to login and back to the originally requested path; the nav shows the session merchant's name, sourced from the session, not a query param. |
| H-1 | History API | `GET /portal/bills` — merchant-scoped, **keyset** page (cursor on `createdAt,id`), filters: date range, `billType`, `source`. Response DTO is an explicit whitelist; contact **masked** (D-48). | A-3 | **Tier-1 (per-merchant scoping + PII projection)** | Two merchants seeded → each sees only its own rows, counts match `SELECT`; `source` filter partitions the set exactly; inserting a bill between two page fetches skips and repeats nothing; a key-set test fails if a field outside the DTO whitelist appears; masked contact only. |
| H-2 | History UI | The history table: filters, cursor paging, empty state, money rendered from `BigInt` paise, timestamps from UTC. Links to detail. | H-1, W-1 | — | Paging forward and back returns the same rows; a merchant with zero bills gets an empty state, not an error; no float appears in any money path (`grep` the component for `parseFloat`/`Number(`); a PG receipt and a direct-API invoice both list correctly. |
| H-3 | Bill detail | `GET /portal/bills/:id` + the detail page: bill facts, line items, link to the public bill page, and the **merchant-visible contact projection in full** (D-48). Not-mine → **404**. | H-1 | **Tier-1 (new PII boundary)** | Another merchant's `billId` → `404`, no body hinting existence; the response contains **exactly** the D-48 field set (key-set test); the public bill page for the same identifier still contains **no** contact data; contact never appears in a log line (deny-test). |
| W-2 | Dashboard | The authenticated landing page: template list, create-invoice entry point, recent bills, all through `MerchantContext`. Reuses the `/demo` panel's components; no new business logic. | W-1, H-1 | — | Every panel shows the session merchant's data; the same page rendered for a second seeded merchant shows disjoint data; `/demo` still renders its own copy unchanged. |
| W-3 | Builder | The Phase-3 builder mounted at `/portal/templates/*`, `merchantId` from the session. `/demo/templates` untouched (D-49). | A-4, W-1 | **Tier-1 (fork-on-write against a real tenant)** | Fork a template from the authenticated route → exactly one new row, `merchantId` = **session** merchant, parent `isHead=false`, `defaultTemplateId` repointed; the Phase-3 UAT script passes against `/portal` and again against `/demo`; a second merchant's `templateId` → `404`. |
| X-3 | Regression | **Verification task, no new code expected:** full cross-merchant probe across every `/portal` endpoint, plus the v1+v2+v3 suites and the §7 immutability regression against a portal-forked template. | W-3, H-3, A-5 | **Tier-1 (tenancy + compliance guarantee)** | Every `/portal` route asked for merchant B's resource while authenticated as A → `404`, zero writes; create a bill, make three portal builder edits → re-resolved bill's rendered block list **unchanged**; all v1/v2/v3 tests green. **If any code change is needed here, that is a finding** — A-3/A-4 were supposed to make scoping structural. |
